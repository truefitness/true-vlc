Avoid pushing TS packets to PSI parser if the transport error indicator is set.
Index: vlc-2.0.8/modules/demux/ts.c
===================================================================
--- vlc-2.0.8.orig/modules/demux/ts.c	2014-02-24 17:31:24.881039096 -0600
+++ vlc-2.0.8/modules/demux/ts.c	2014-02-24 17:31:41.453039410 -0600
@@ -1089,13 +1089,15 @@
             {
                 if( p_pid->i_pid == 0 || ( p_sys->b_dvb_meta && ( p_pid->i_pid == 0x11 || p_pid->i_pid == 0x12 || p_pid->i_pid == 0x14 ) ) )
                 {
-                    dvbpsi_PushPacket( p_pid->psi->handle, p_pkt->p_buffer );
+                    if( !(p_pkt->p_buffer[1] & 0x80) )
+                        dvbpsi_PushPacket( p_pid->psi->handle, p_pkt->p_buffer );
                 }
                 else
                 {
                     for( int i_prg = 0; i_prg < p_pid->psi->i_prg; i_prg++ )
                     {
-                        dvbpsi_PushPacket( p_pid->psi->prg[i_prg]->handle,
+                        if( !(p_pkt->p_buffer[1] & 0x80) )
+                            dvbpsi_PushPacket( p_pid->psi->prg[i_prg]->handle,
                                            p_pkt->p_buffer );
                     }
                 }
