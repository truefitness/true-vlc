diff -u -x depcomp -x ltmain.sh -x configure -x va.h -x '*.m4' -x '*.in' -x '*.o' -x compile -r vlc-2.0.8/include/vlc/libvlc_media_player.h /usr/src/vlc-2.0.8/include/vlc/libvlc_media_player.h
--- vlc-2.0.8/include/vlc/libvlc_media_player.h	2013-06-17 17:07:53.000000000 -0500
+++ /usr/src/vlc-2.0.8/include/vlc/libvlc_media_player.h	2013-11-18 13:02:29.199680309 -0600
@@ -294,6 +294,12 @@
                                            unsigned *pitches,
                                            unsigned *lines);
 
+typedef unsigned (*libvlc_video_format2_cb)(void **opaque, char *chroma,
+					    unsigned *width, unsigned *height,
+					    unsigned *pitches,
+					    unsigned *lines, unsigned *sar_num,
+					    unsigned *sar_den);
+
 /**
  * Callback prototype to configure picture buffers format.
  *
@@ -358,6 +364,12 @@
                                         libvlc_video_format_cb setup,
                                         libvlc_video_cleanup_cb cleanup );
 
+LIBVLC_API
+void libvlc_video_set_format2_callbacks( libvlc_media_player_t *mp,
+					 libvlc_video_format2_cb setup,
+					 libvlc_video_cleanup_cb cleanup );
+
+
 /**
  * Set the NSView handler where the media player should render its video output.
  *
diff -u -x depcomp -x ltmain.sh -x configure -x va.h -x '*.m4' -x '*.in' -x '*.o' -x compile -r vlc-2.0.8/lib/libvlc.sym /usr/src/vlc-2.0.8/lib/libvlc.sym
--- vlc-2.0.8/lib/libvlc.sym	2011-12-08 12:00:25.000000000 -0600
+++ /usr/src/vlc-2.0.8/lib/libvlc.sym	2013-11-18 13:29:10.719770174 -0600
@@ -205,6 +205,7 @@
 libvlc_video_set_deinterlace
 libvlc_video_set_format
 libvlc_video_set_format_callbacks
+libvlc_video_set_format2_callbacks
 libvlc_video_set_key_input
 libvlc_video_set_logo_int
 libvlc_video_set_logo_string
diff -u -x depcomp -x ltmain.sh -x configure -x va.h -x '*.m4' -x '*.in' -x '*.o' -x compile -r vlc-2.0.8/lib/media_player.c /usr/src/vlc-2.0.8/lib/media_player.c
--- vlc-2.0.8/lib/media_player.c	2011-12-08 12:00:25.000000000 -0600
+++ /usr/src/vlc-2.0.8/lib/media_player.c	2013-11-18 14:00:03.619874158 -0600
@@ -407,6 +407,7 @@
     var_Create (mp, "vmem-display", VLC_VAR_ADDRESS);
     var_Create (mp, "vmem-data", VLC_VAR_ADDRESS);
     var_Create (mp, "vmem-setup", VLC_VAR_ADDRESS);
+    var_Create (mp, "vmem-setup2", VLC_VAR_ADDRESS);
     var_Create (mp, "vmem-cleanup", VLC_VAR_ADDRESS);
     var_Create (mp, "vmem-chroma", VLC_VAR_STRING | VLC_VAR_DOINHERIT);
     var_Create (mp, "vmem-width", VLC_VAR_INTEGER | VLC_VAR_DOINHERIT);
@@ -845,6 +846,14 @@
     var_SetAddress( mp, "vmem-cleanup", cleanup );
 }
 
+void libvlc_video_set_format2_callbacks( libvlc_media_player_t *mp,
+                                        libvlc_video_format2_cb setup,
+                                        libvlc_video_cleanup_cb cleanup )
+{
+    var_SetAddress( mp, "vmem-setup2", setup );
+    var_SetAddress( mp, "vmem-cleanup", cleanup );
+}
+
 void libvlc_video_set_format( libvlc_media_player_t *mp, const char *chroma,
                               unsigned width, unsigned height, unsigned pitch )
 {
diff -u -x depcomp -x ltmain.sh -x configure -x va.h -x '*.m4' -x '*.in' -x '*.o' -x compile -r vlc-2.0.8/modules/video_output/vmem.c /usr/src/vlc-2.0.8/modules/video_output/vmem.c
--- vlc-2.0.8/modules/video_output/vmem.c	2011-12-08 12:00:27.000000000 -0600
+++ /usr/src/vlc-2.0.8/modules/video_output/vmem.c	2013-11-18 15:02:20.796083870 -0600
@@ -103,6 +103,8 @@
 
 typedef unsigned (*vlc_format_cb)(void **, char *, unsigned *, unsigned *,
                                   unsigned *, unsigned *);
+typedef unsigned (*vlc_format2_cb)(void **, char *, unsigned *, unsigned *,
+				   unsigned *, unsigned *, unsigned *, unsigned *);
 
 static picture_pool_t *Pool  (vout_display_t *, unsigned);
 static void           Display(vout_display_t *, picture_t *, subpicture_t *);
@@ -125,6 +127,7 @@
 
     /* Get the callbacks */
     vlc_format_cb setup = var_InheritAddress(vd, "vmem-setup");
+    vlc_format2_cb setup2 = var_InheritAddress(vd, "vmem-setup2");
 
     sys->lock = var_InheritAddress(vd, "vmem-lock");
     if (sys->lock == NULL) {
@@ -141,16 +144,25 @@
     /* Define the video format */
     video_format_t fmt = vd->fmt;
 
-    if (setup != NULL) {
+    if (setup != NULL || setup2 != NULL) {
         char chroma[5];
+	unsigned int sar_num, sar_den;
 
         memcpy(chroma, &fmt.i_chroma, 4);
         chroma[4] = '\0';
         memset(sys->pitches, 0, sizeof(sys->pitches));
         memset(sys->lines, 0, sizeof(sys->lines));
 
-        sys->count = setup(&sys->opaque, chroma, &fmt.i_width, &fmt.i_height,
-                           sys->pitches, sys->lines);
+	sar_num = fmt.i_sar_num;
+	sar_den = fmt.i_sar_den;
+
+	if (setup2)
+	  sys->count = setup2(&sys->opaque, chroma, &fmt.i_width, &fmt.i_height,
+			      sys->pitches, sys->lines, &sar_num, &sar_den);
+	else
+	  sys->count = setup(&sys->opaque, chroma, &fmt.i_width, &fmt.i_height,
+			     sys->pitches, sys->lines);
+
         if (sys->count == 0) {
             msg_Err(vd, "video format setup failure (no pictures)");
             free(sys);
diff -u -x depcomp -x ltmain.sh -x configure -x va.h -x '*.m4' -x '*.in' -x '*.o' -x compile -r vlc-2.0.8/src/video_output/display.c /usr/src/vlc-2.0.8/src/video_output/display.c
--- vlc-2.0.8/src/video_output/display.c	2013-07-25 06:42:42.000000000 -0500
+++ /usr/src/vlc-2.0.8/src/video_output/display.c	2013-11-15 15:21:32.568125886 -0600
@@ -107,8 +107,8 @@
 
     /* Picture buffer does not have the concept of aspect ratio */
     video_format_Copy(&vd->fmt, fmt);
-    vd->fmt.i_sar_num = 0;
-    vd->fmt.i_sar_den = 0;
+//    vd->fmt.i_sar_num = 0;
+//   vd->fmt.i_sar_den = 0;
 
     vd->info.is_slow = false;
     vd->info.has_double_click = false;
